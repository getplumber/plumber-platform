

### Fixes
- After an upgrade, the "update available" notice no longer shows an older version than the one you just installed; the check now runs at start-up and a result from a previous version is never reused.
