

### Features
- The push carries a bill of materials and the dependencies routes are specified (spec 2026-09-23)
- Dependency resources and usages, the run keeps its bill of materials (migration 0164)
- A default-branch push projects its bill of materials into the dependencies graph (bounded)
- The project snapshot projects its ci/cd variables into the dependencies graph
- Who uses a resource and a project's bill of materials, visibility-cut (spec section 6)
- Org settings carry community_link_hidden, /me carries org_ui
- Store and serve the community link toggle
- The second cut of the Plumber brand, and the social card on the static card's stage

### Fixes
- Log org_id when the org_ui read for /me fails
