

### BREAKING
- Remove IssueDetail.fixes, history is the only per-issue fix surface
- Tolerate a pre-pin unknown key and report it as a warning (row 67)
- Scope official reads the org-curated flag, gitlab_maintained carries GitLab's levels (row 82)

### Features
- P0 step 1 — control-plane skeleton
- P0 step 2 — versioned, forward-tolerant ingestion contract
- P0 step 3 — Postgres storage with content-addressed blob seam
- P0 step 4 — CI OIDC verification (ADR-0003)
- P0 step 5 — POST /ingest endpoint
- P0 step 6 — structured AI-trace capture (IA §5)
- P0 step 7 — on-site trace scrubbing (ADR-0014)
- Hosted Plumber score service — push + live badge
- Localize report "last update" time to the viewer's timezone
- Redirect the root path to the Plumber score docs
- Round the corners of the Plumber Score badge
- P0 backend — ingestion rail + project-context endpoint
- Minimal read-only config schema (policies + assignments) + resolution query
- Resolve the real assigned policy set from DB config (default fallback preserved)
- Config-driven structural caps on the untrusted push body (ADR-0033)
- 415 on wrong content-type, generic 422, per-reason reject metrics (ADR-0033)
- Add social card image rendering and SEO metadata
- Normalize the claim ref into branch/tag + carry claim sha (ADR-0003)
- Persist the claim ref, body ref is diagnostic + capped (ADR-0003)
- Project settings-snapshot store (migration 0003 + sqlc + Store methods) — ADR-0021
- Serve the stored settings snapshot (I4 cache read) — ADR-0021
- GitLab collector adapter importing the CLI collector via the injected client — ADR-0021
- One rule-J HTTP client per (provider,instance) — ADR-0025
- Postpone server-side score recompute, accept the pushed score
- Rebuild the dashboard on a modern UI and data stack (#39)
- Multi-instance foundation - organizations, git_instances, groups (P1-c, ADR-0025)
- Policy content model - requirements + control instances (P1-d, ADR-0018/0006)
- Portfolios + group: resolution engine (P1-d, ADR-0024)
- Policy assignment via projects + portfolios, effective-policy resolution (P1-d, ADR-0006)
- Config write ops + governance event marks affected projects stale (P1-d, ADR-0018/0004)
- AES-256-GCM seal/open + operator token-encryption key (P1-b, rule G)
- Token metadata columns + rule-G token queries (P1-b)
- Validate-on-save + sealed storage + server-only collection read (P1-b, rule G/J)
- Collection token from git_instances (rotation-immediate); drop dead env knobs (P1-b)
- Redis-backed sessions + single-use PKCE verifiers (ADR-0022)
- Users table + per-instance OAuth app columns with rule-G query split (P1-a)
- Per-instance OAuth login with PKCE, sealed user tokens, redis sessions (P1-a)
- Session middleware + provider-token auto-refresh (P1-a, rule G)
- Live role derivation + FilterAssets visibility port (P1-a, RBAC red line)
- OrgAdmin instance settings - token rotation + OAuth app (P1-a/P1-b, ADR-0030)
- SSE per-project RBAC filtering behind user sessions (P1-f, closes #22)
- Same-org DB backstop on config link tables (task #23, ADR-0026 rule 4 pattern)
- Full config write-op set with governance events and affected-return (task #23)
- Config write API surface + settings 409 (task #23, ADR-0030)
- Policy CRUD, content, assignments behind Maintainer RBAC (task #23)
- Portfolio CRUD, members, rules behind Maintainer RBAC (task #23)
- Wire the config write API; config governance rail in checkpoint (task #23)
- Add controls visibility and CLI version to report page
- River queue infrastructure with transactional enqueue (ADR-0028)
- Sync runs + discovery inventory columns (ADR-0021)
- Asset discovery, detection, reconciliation-lite (ADR-0021, rule J/G)
- Sync + snapshot-refresh jobs, daily cron, activity-driven enqueue (ADR-0021/0028)
- Instance create, sync trigger/status, visibility-filtered inventory (ADR-0021)
- Wire syncapi; onboarding/sync rail in checkpoint (ADR-0021)
- Serve the OpenAPI contract, Member-gated (+ PLUMBER_SPEC_PUBLIC dev toggle)
- Shared base_url guard, opt-in private-range rejection; body limit, 409 wording, 403 alignment (#26)
- Bind OAuth state to a short-lived browser cookie (#26 login CSRF)
- Instance-scoped visibility pool; org-keyed SSE routing (#24)
- Per-project visibility on config reads (#24, RBAC red line)
- Project-level variable metadata + schema_version (values never stored)
- Snapshot collection timebox; ADR-0007 token-scope correction; rail proves merged_yaml end to end
- Dev-only bootstrap for the first local browser login
- The variable-value structural guard (ADR-0033 extension, metadata-only end to end)
- The versioned CI config content digest (ADR-0034 rule 1)
- Resolution anchor + the digest cache (ADR-0034 rules 2/5)
- The bounded digest-warm resolve endpoint (ADR-0034 rule 4, INVARIANTS rule K)
- Resolve-cache retention; the divergent-branch rail (ADR-0034)
- The variable-value guard rejects by provenance, not shape (ADR-0033 extension v2)
- Schema, identity computation, the corrected identity-fields seed (ADR-0026 rules 1-4)
- The same-tx upsert engine with evaluability-aware closure (ADR-0026 rules 5-8)
- Rule 9 across every emptying op; the SSE issues_closed carrier (ADR-0026 rule 9, gate #28)
- The issue lifecycle rail end to end (ADR-0026)
- Identity comes from the CLI's shared recipe; drop the seeded table (ADR-0026)
- The CI auth boundary and malformed config stop failing silently
- Count the abandoned sync runs the panel reinterprets (quality gate)
- Instance-scope schema and cached scope-aware role derivation
- Scope-driven discovery and the pending group lifecycle
- Wire the role cache into main and seed scope in test fixtures
- The derived coverage classification (ADR-0019 rule B)
- Collect the provider default branch (ruling #10 groundwork)
- The ruling-#10 branch-conditional onboarded writer
- Derived coverage field + filter on the projects inventory
- GET /overview, the coverage rollup over the visible set
- Seed the three policy packs, translated to v2 controls (ruling #4)
- The ADR-0019 watchdog sweep, sync_runs half (ruling 2026-08-10)
- Token rotation fans out per-instance snapshot refreshes (ruling 2026-08-10)
- The production first-config command (ruling 2026-08-10)
- One control_type per policy + the seeded dev-tier drop (ruling 2026-08-11)
- The running producer columns + writers (ADR-0019, migration 0021)
- The TriggerWorker - refresh-before-trigger + retry-at-trigger-time (REQ, ADR-0021 rule 1)
- The watchdog freshness sweep - running is never terminal (ADR-0019)
- The four recheck endpoints - 202+SSE, leak-safe visibility-intersected counts
- Extract the pinned component path+ref from the CI config (spec s1)
- The component version facts, blank-guarded (migration 0023, spec s2)
- Once-per-run latest resolution + the three comparison tiers (spec s3)
- The outdated producer - stale-beats-outdated + the adoption rule (spec s4)
- The component_clear flag wins over the blank guard (clear-on-absence spec s2)
- Clear component facts on confirmed absence + ruling records (clear-on-absence spec s3/s4)
- The issues-console queries + history actor/comment (spec s1/s2/s6)
- The issues console read surface (list + detail, spec s1/s5) - green
- The triage writes - batch status, comment, owner (spec s2/s4) - green
- The fixes table, the fix trigger type, and the write-token accessor (B1 spec s2/s3)
- The catalog frame + the two branch-protection fixers (B1 spec s1/s6)
- The fix trigger + history + the fixable surface - green (B1 spec s4)
- The FixWorker - user-token fixes + the flow-4 chain (B1 spec s5)
- Targets from the DB policy config - the provenance rulings (B2 spec s1)
- Mrops + the CI transforms (B2 spec s2)
- The three MR fixers - server-resolved targets + group choice (B2 spec s3)
- The merge endpoint + sync-observed merges - flow 5 (B2 spec s4/s5)
- Pre-flight branch recording + the deleted-MR miss counter (rulings 2026-08-12)
- Flow-5 running transitions publish project.update (ruling 2026-08-12)
- Migration 0029 + the catalog storage layer (ADR-0032 slice 1)
- The template crawler ported onto the provider interface (ADR-0032 spec s3)
- The River crawl worker + three update lanes (ADR-0032 spec s4)
- The OrgAdmin-gated catalog registration API (ADR-0032 spec s5)
- Org-wide template reads, label CRUD, and the include-path resolver query (slice 2)
- Serve the imported catalog - templates, versions, labels (slice 2)
- The catalog resolves required templates, the last-segment guess is deleted (ruling 2026-08-14)
- Migration 0030 + the onboarding store + the component setting
- The Maintainer-gated onboard endpoint + the modal read
- Publish project.update on the onboard policy write (SSE parity ruling)
- The onboarding MR worker (rule G, immutable pin, in progress)
- The merge endpoint + the rail closes the onboard action
- Migration 0031 + the authorized-maintainer-group store
- Derive Maintainer from authorized groups (rulings 1 and 2)
- Settings authorization CRUD + the Maintainer routes go live
- The .plumber.yaml canonicalizer and matcher (ruling 2026-08-11)
- Offer the .plumber.yaml policy on the onboard modal read
- Import the .plumber.yaml policy inside the onboard transaction
- Migration 0032 + the fan-out storage layer
- The first org-settings surface (GET Member / PUT Maintainer)
- The drain worker (K per org per tick, auto_fanout provenance)
- The interactive River queue (R7-2, ADR-0021 amended)
- The providerlimit package (rule J, the v1 port)
- The limiting transport, the CA bundle, the JWKS seam (rule J)
- Class threading and honest sheds at every call site
- Key policies by uuid end-to-end (the 2026-08-10 ruling)
- The gate block on the push response (enforcement, never evaluation)
- The input readers + the component-spec hoist (design s2)
- The strict correct-inputs reconciliation (the 2026-08-11 ruling + the 2026-08-16 absence ruling)
- The watchdog reaps stranded pendings + the per-queue backlog gauge (recorded follow-ups)
- Template_file_amount written + checkpoint river cleanup + the records (recorded follow-ups)
- The org toggle + the comment renderer + the delta queries (ADR-0031 row 2)
- The mr-comment worker + the ingest enqueue (updated, never duplicated)
- Wire the worker + the rail + the records (ADR-0031 row 2 shipped)
- The org toggle + anchor column + renderer + counts (ADR-0031 row 3)
- The rolling summary-issue worker + the ingest enqueue (one issue, updated)
- Wire the worker + the rail + the records (the ADR-0031 inventory complete)
- The pinned-CLI scoring adapter + per-policy codeCounts (ADR-0020 restore, #21)
- Store the recomputed score, recomputed=true, divergence observed (I2 restored)
- The rails on recomputed truth + the records (the ADR-0020 deviation closed)
- The audit list queries + the contract addition (journey: govern the instance, step 4)
- The three audit read endpoints (OrgAdmin, read-only)
- The badge org toggle + the auto-assign toggle (migration 0038, five-field settings)
- Issue owner auto-assign (I4-clean river worker, rule-J client, fills unassigned only)
- The badge URL carries org slug + instance (migration 0039, ruling 4)
- Unboarded pushes never update the display; an archived push re-checks (ruling 2)
- Provider-deleted joins the accept-no-op set (the ruling's third row, design corrected)
- The branch and commit prefixes in org settings (migration 0040, ruling 8)
- Every MR writer honors the org prefixes
- The unboard endpoint marks unboarding and queues the removal (migration 0041)
- Citransform removes the Plumber include, idempotent and refusing rather than guessing
- The removal MR, the merge endpoint, and unboarded projects are onboardable again (ruling 2026-08-20)
- Org SMTP settings, encrypted and test-sent at save (migration 0042)
- The CSV, PDF and zip generators (ported from legacy, v2 data)
- Scope assembly reusing the console's own read paths
- Async export generation and download
- The scheduled org report, pausable, with honest delivery status
- Expose visibility as a SQL-expressible predicate
- Batched issue reads for the ingest-batch slice (Task 2)
- Batched issue writes for the ingest-batch slice (Task 3)
- Batch resolveResultPolicyIDs's id/name lookups (Task 4)
- Wire the batched ingest path, prove it differentially (Task 5)
- Paginated portfolio-resolved-set and policy-assignment storage queries (Task 1)
- Capped preview + counts on getPortfolio/getPolicy, paginated project sub-resources (Task 2)
- The unboard cancel surface (unboard-stuck-unboarding-no-recovery fork c, half 2)
- The export-notify SSE completion event (report.export)
- Config_audit storage layer - migration, insert, read lanes, append-only guard (Task 1)
- Hooks, enumeration guard, lane endpoint, contract (Task 2)
- Lower the audit read gate to Maintainer, per the unlock-audit (Task 1+2)
- Migration 0057 - projects.newest_run_unscorable marker
- Newest_run_unscorable on InventoryProject, listProjects
- Store the GitLab listing's CI provider facts (migration 0058)
- ClassifyCIConfigPath helper + table test
- GA slice 1 onboard-time guards + warnings + empty-offer fix
- GA slice 1 onboard MR gets smarter - stage/allow_failure inputs + prerequisites description
- Mr_opened candidate pools for onboarding/unboarding observation (GA slice 3, task 1)
- Observe onboarding/unboarding MRs until they converge (GA slice 3, task 1)
- GA slice 8b - the ruled inbound sliding-window rate limiter
- GA slice 9 Task 1 - LIMIT/OFFSET + Count queries for the five converted lists
- GA slice 9 Task 1 - envelope-everywhere + strict-query contract updates
- GA slice 9 Task 1 - envelope conversions + strict-query wiring + listqueryguard
- GA slice 9 Task 2 - real search + the missing filters
- GA slice 9 Task 3 - observable, recoverable SSE
- GA slice 10 Task 1 - title/owner, score, scope section, CJK font fallback
- GA slice 10 Task 1 - the 202/200 export status discriminator
- GA slice 10 Task 2 - org timezone, grid-anchored schedule ticks, next_run_at
- Score-history rollup tables + queries (ADR-0022 GA slice 11 Task 1)
- Shared as-of/live compute + hourly rollup worker + startup backfill (ADR-0022)
- Score-history read endpoints (ADR-0022 GA slice 11 Task 1)
- PLUMBER_RETENTION_DAYS + retention mechanics knobs (ADR-0022, GA slice 11 Task 2)
- Retention SQL queries + guard-allowlist entries (ADR-0022, GA slice 11 Task 2)
- Weekly runs retention + downsampling pass (ADR-0022, GA slice 11 Task 2)
- Wire the weekly retention River worker (ADR-0022, GA slice 11 Task 2)
- Project_snapshots + sync_runs retention SQL queries (ADR-0022, GA slice 11 Task 3)
- Project_snapshots + sync_runs aging legs (ADR-0022, GA slice 11 Task 3)
- Visibility observed - migration 0079, sync captures it, no new provider calls
- The badge enablement path - PUT /api/v1/projects/:id/badge
- Adoption re-detection plumbing (implements R6-2)
- Ldflags-stamped version + git SHA (GA slice 13 Task 1)
- Update_check_status singleton table (migration 0082)
- The honest daily update-check (GA slice 13 Task 1, J02-H2)
- Daily update-check River periodic worker + config knobs
- GET /version + UpdateCheckStatus/UpdateCheck/VersionInfo
- GET /api/v1/version + main.go wiring
- Catalog label update-in-place + last-sync signal on the repo list (J09-H2)
- Label update route, repo list last-sync field, semver-first version ordering (J09-H2, J09-E2)
- Warn on a below-floor GitLab version at token validation (J03-E8)
- DB SSL cert knobs + Redis URL knob (J01-R5, J01-R6)
- Wire the DB SSL cert knobs into the DSN (J01-R5)
- The shared go-redis options builder (J01-R6)
- Route the store's client through the shared redisopts builder (J01-R6)
- Route NewClient's base options through the shared builder (J01-R6)
- Wire every Redis client construction through the shared builder (J01-R6)
- J03-E9 - split /projects archived/provider_deleted into independent opt-ins
- Additive OnboardMergeFailureReason members for J06-F4 (GA slice 16 Task A)
- OpenMR 403 classification, DeleteBranch, and the not-ready residual (J06-F12, J06-F6)
- RecordOnboardingMergeError - the mr_opened-preserving diagnostic write (J06-F4)
- Merge-refusal classification honesty (J06-F12 residual, J06-F4)
- Compensating branch cleanup on a terminal OpenMR refusal (J06-F6)
- R7-2 completion - manual sync, mr_comment, summary_issue ride InteractiveQueue
- Trigger worker walks a bounded window of retry-target pages (J12-E7 half A)
- Reconcile's component-outdated refresh covers every non-terminal, pinned row (J12-E7 half B)
- Component write-time validation + onboarding-attempt visibility (J11-H1)
- PLUMBER_SSE_MAX_PER_USER concurrent-stream cap (X4-6)
- The portfolio's own numbers + policy portfolio reach (J08-H1)
- The stamped backend image (release-process spec)
- The frontend image takes the monoversion stamp
- The full-stack smoke gate
- The on-demand release workflow + changelog config
- Default the manifest URL, add the off sentinel
- Smtp ca bundle knob trusts a private-ca mail relay (PLUMBER_REPORT_SMTP_CA_BUNDLE, J17-E6)
- Unauthenticated login-capable instance discovery read (spec A)
- Serve session identity on /me and capture the provider username (spec B)
- Stored done flag + derived wizard signals (spec C)
- Config.update and sync.update - recorded rule-A vocabulary extension (spec D)
- Publish sync.update after EnqueueSync's own commit
- Pending onboarding, counts and last_run_at on the inventory row (spec E)
- Embedded immutable create-from-template catalog (spec F)
- Read-only control library derived from the pinned CLI (spec G)
- Add #368 slice-1 snapshot keys and resolved-config includes/ci_errors to the contract
- Serve raw_config, merged_yaml_status and ci_errors (#368 ask 5)
- Serve project_details core fields (#368 ask 4, partial)
- Enrich includes with latest_version and ref_is_ambiguous (#368 ask 2)
- Serve includes and ci_errors on resolved-config, cache includes (migration 0089, #368 ask 3)
- Delete include verdicts, serve ref existence observations (#368 slice 2)
- Serve source_catalog verbatim on component includes (#368 slice 2)
- Serve security_policy_project with degrade-on-unknown (#368 slice 2 B)
- Observation keys and security_policy_project in the snapshot contract (#368 slice 2)
- Serve includes jobs and jobs_known via the cli attribution (#368 ask 3)
- Serve project_details merge settings (#368 ask 4, tier c)
- Jobs, merge settings and includes_jobs in the snapshot contract (#368 tier c)
- Frontend batch 2 surface (summary scores, aggregate history, inventory additions, breakdown, instances)
- Projects activity/languages columns and the open-issues partial index (0090, 0091)
- Collect last_activity_at and languages (frontend batch 2 item 7)
- Live scores on policy and portfolio list summaries (frontend batch 2 item 1)
- Aggregate score-history reads for policies and portfolios (frontend batch 2 item 2)
- Policy/portfolio id arrays and open issue count on the inventory row (frontend batch 2 items 3, 8)
- Server-side inventory filters (frontend batch 2 item 4)
- Single-project inventory read (frontend batch 2 item 5)
- Per-policy score breakdown on portfolio detail (frontend batch 2 item 6)
- Member-readable connected instances with base_url (frontend batch 2 item 9)
- Frontend batch 3 surface (org score, control breakdown, trend, unboarding, sort, users, me)
- Batch 3 migrations and queries (org score days, run control results, unboarding index, severity index)
- Org-wide score, rollup and history (batch 3 ruling 1)
- Ingest-time per-control rollup and the policy control breakdown read (batch 3 ruling 2)
- Top projects read (batch 3 ruling 3)
- Open-issues trend read (batch 3 item 1)
- Pending_unboarding on the inventory rows (batch 3 item 3)
- Sort params on the issues list (batch 3 item 4)
- Member user resolution and /me identity fields (batch 3 items 5, 6, 9)
- Licence, ai settings, wizard signals, and component verification surface
- Offline licence validation ported from the legacy model
- Sealed org licence ciphertext storage (migration 0097)
- Effective-licence service over sealed org storage
- Licence read and apply-key surface
- Licence enforcement gates (onboarding cap, portfolio level)
- Byo-llm ai settings surface (capture-only ruling intact)
- Wizard signals and surfaced component verification
- Licence-signer keygen and sign cli
- Degraded-collection flags and raw config write path
- Degraded-collection badge on runs and displayed scores (X3-20)
- Add the distribution metrics tracker
- Batch-4 surfaces (provider id, order, control names, synced groups, component flags)
- Inventory provider id and top-projects order (items 7, 1)
- Component-repo detection cache and org-curated official flag (item 4)
- Control display names reflected off the pinned cli (item 2)
- Synced-groups listing for the grant picker (item 3)
- Rulings batch surfaces (other_count, badge marker, group instance, provider token)
- Other_count bucket for unrecognized finding statuses
- Degraded-collection marker on the public grade (x3-20 completion)
- Synced-group instance scope filters and the provider-token handout
- Batch-5 surfaces (control groups rename, min_points, names, freshness, policy projects)
- Control-group rename wiring and the dual-key transition (item 9)
- Per-policy min_points enforcement threshold (items 3+4)
- Project display names, freshness filter, policy search (items 1, 2, 7)
- Per-policy project results on the policy detail (item 11)
- Batch-6 surfaces (portfolio projects rename, sort, tree, breakdown filter, version check)
- Portfolio members rename to projects (ruling 3)
- Projects sort with the widened keyset cursor (item 16, the ruled shape)
- Anchored path prefix and direct-children listing (item 17)
- Member root group, synced-groups search, degraded flag on sse (items 12, 9, 11)
- Portfolio-scoped control breakdown (item 18)
- Atomic official set and the component version check (items 10, 5)
- Activate daily collection on main
- Policy scope filter, preview endpoint, scope_filtered flag
- Collect project topics on the sweep listing (spec s2)
- Scope-filter predicate in the effective-policy resolution (rulings 2/4/5)
- Scope filter writes, scope_filtered, governed policy-projects listing
- Scope preview via rolled-back saved-path resolution, unclassified counts
- Re-stale on scope-filter edits; inventory justifications (spec s3)
- Stamp was_default_branch on runs at ingest (spec s1)
- Default-branch display ownership with newest-any fallback (spec s2)
- Durable issues from default-branch runs only (spec s3)
- As-of selection follows display ownership (spec s6)
- Branch-model surface (worst trio, fifth sort key, score_source, branches read)
- Live-branches read and score_source (spec s4)
- Worst-live-branch trio, aging, fifth sort key (spec s5)
- Dismissed-issue list on context, dismissed marker on findings
- Serve dismissed-issue identities on context (spec s1)
- Dismissed-marker authority rule (spec s3)
- Count installs, not checksums, and stop mixing clones in
- Issue fallback mirror for unknown/pre-sync default branches (row 16)
- Combined groups and projects tree read (issue 91 item 19)
- Sort templates by name or most recent (questions row 6)
- Embed the full project object on the issues list and detail
- Remove the orphaned PUT /catalog/official batch replace-set (row 21)
- Sort policy projects by score or path (row 20)
- Multi-value issues console filters, mirrored and byte-guarded (issue 91 ask 27)
- Status, severity, control_type and owner accept repeated values (issue 91 ask 27)
- Bounded honest display payload on every issue row (issue 91 ask 25)
- Requiring policy refs, analysis-in-progress, owner avatar on issue rows (issue 91 ask 26)
- Freshness summary on the portfolio detail, shared with the coverage rollup (issue 91 ask 22)
- Freshness summary on the policy detail over the governed set (issue 91 ask 22)
- Add ProjectIDsForInventoryPage, the lean id projection of the inventory page (issue 91 ask 23)
- Add GET /projects/ids, a lean path-ordered id projection with a raised cap (issue 91 ask 23)
- Derive the control library from the pinned CLI catalog: id, category, providers, default_config (ask 24)
- Add GET /issue-types serving the pinned CLI issue-type catalog (rows 9, 10)
- Serve the finding's ISSUE code on issue rows for the issue-types join (row 30)
- Stale a project whose topics or languages move it across a scope-filter boundary (row 32)
- Flag a topics change on the listing upsert so a scope-boundary cross stales the project (row 32)
- Carry issues_closed for non-staled projects on every unassign path without asserting stale (row 33)
- Assign policies from the portfolio side (create policy_ids, PUT portfolio policies, row 34)
- Add applies_to (assigned | all_onboarded) to policies: column, write side, preview, contract (row 35)
- All_onboarded policies govern every onboarded project in the governed blocks and the rule-9 set (row 35)
- Serve all_onboarded policies through the effective-policy resolver and its batch twin (row 35)
- List the whole coverage on the policy projects page under all_onboarded (row 35)
- Compute returns the formula's letter, final points and signed raw points (row 40)
- Migration 0126, score_points means final points and raw_points carries the signed raw deficit (row 40)
- Store the formula's final and raw points, gate on final points (row 40)
- Additive final_points and raw_points, score_points means final points
- Pin the cli at v0.4.58 and take the score band from its export
- Policy_ids and the policy_id filter list every governing policy (row 36)
- Pace policy, portfolio and org re-check sweeps against the resolver cap (row 50)
- Serve latest_fix_status on IssueSummary and IssueDetail (row 48)
- Publish fix.update SSE doorbell on every fix status transition (row 48)
- The GET /projects facet family on the policy and portfolio project lists (row 49)
- Serve the instance's CI/CD components from the sync sweep, GET /catalog/components (row 47)
- The resolve lane observes includes like the snapshot lane
- Reflect the pinned CLI's nullable on ControlLibraryField (row 52)
- Store no score when a pushed policy result has no evaluated control (row 45)
- Accept a pushed run that evaluated nothing, with its marker and reason (row 63)
- Record a linked run that evaluated nothing instead of losing it (row 63)
- Refresh the project's component ref from the push (row 58)
- Serve a run's nothing-evaluated reason on the analyses lane (row 63)
- Serve the project display name on the fix audit rows (row 59)
- Merge status moves and fix attempts into one issue history with actors (row 60)
- Validate a control config against the served schema tree (row 56)
- Validate a policy control config at write time against the served schema (row 56)
- History fix entries carry the merge-request reference (row 60)
- Parse the root CI file with the CLI's multi-document parser (row 54)
- Flag a changed component ref and enqueue its re-detect in the push transaction (row 69)
- Re-detect one project's component facts from a queued job (row 69)
- Canonicalize a control config's keys onto the served names (row 66)
- Canonical served spelling, and skip a control the served schema refuses (row 66)
- Report the controls the plumber.yaml import skipped (row 66)
- Stamp the pinned cli version on every stored control (row 67)
- Re-key stored control configs onto the served spelling and stale what changed (row 66)
- Carry catalog component names, namespace and verification level (row 70)
- Scope filter on the components read, byte-shared and explain-gated (row 70)
- Serve component names, namespace, verification level and the scope filter (row 70)
- Portfolio excluded projects table and the two resolver subtractions (row 71)
- Every portfolio membership reader subtracts the excluded set (row 71)
- Full-replace write for portfolio excluded projects, rule-matched only (row 71)
- Members-PUT prunes an excluded project's exclusion on re-add (row 71)
- The excluded-projects PUT and excluded_projects on the portfolio detail (row 71)
- Gitlab_maintained scope on GET /catalog/components, official documented as org-curated (row 82)
- Any_policy_created signal on GET /onboarding/status (row 72)
- Over_limit and remaining on the licence status, licence_exceeded on the onboarding status (row 87)
- Hide provider-deleted projects' issues by default, provider_deleted=true opts in (row 89)
- Store the provider's project creation date and filter the listings by it (row 84)
- Has_onboarding_error filter on the project listings (row 93)
- First_detected and updated period filters on GET /issues (row 91)
- The /issues/summary read with by_status and by_severity counts under the list filters (row 92)
- Recommendation with the CLI's target version on ISSUE-403 summaries and detail (row 94)
- Project_name on FixHistoryEntry and AuditAnalysisEntry (row 95)
- Component_ref optional, component_resolution on the instance settings and the version check (row 90)
- The onboard job resolves the newest component tag at MR time and records it (row 90)
- Migration 0140 re-stamps the identity recipe version to 5 on unchanged codes (row 85)
- Audit rows carry actor_id, actor_avatar_url and avatar_url (row 104)
- Fix and config rows carry actor_id and actor_avatar_url, user rows carry avatar_url (row 104)
- The trend hides provider-deleted projects like the listing, provider_deleted opts them in (row 96)
- A manual sync records the user who triggered it (row 105)
- Sync entries on the analyses lane carry actor_id and actor_avatar_url (row 105)
- The pending onboarding carries pipeline_has_plumber_job, stored and served (row 103)
- The MR observation reads the onboarding pipeline's job list and stores pipeline_has_plumber_job (row 103)
- Migration 0143 re-stamps the identity recipe version 5 to 6 except ISSUE-405, 408 and 417 (row 107)
- A fix lints the transformed CI configuration before its first write (row 113)
- Avatar length cap at login, last activity touched on our writes and ingest (rows 106, 109)
- A fresh install seeds no policy packs, the packs stay templates (row 111)
- A project whose provider activity has not moved is skipped by reconcile and redetect (row 108)
- The per-project loops run under a bounded worker pool on the shared client and limiter (row 108)
- An onboarded project with no result carries the reason from its default-branch pipeline (row 110)
- A linked run waits in budget for a first-seen include observation, exceeded waits counted (row 119)
- A policy applies to all, all_onboarded accepted as an alias for one release (row 112)
- An all policy governs every project of the org within its scope, covered stays onboarded (row 112)
- A transient observation verdict cools the awaited wait off for ten minutes (row 124)
- The database pool default is 25, refusal and avatar-drop counters (row 122)
- A project carries whether its component version check ran, and why not (row 118)
- The inventory filters on version_check like on deployment_status (row 118)
- Project.update carries the version check pair under the same omission rule (row 118)
- A read endpoint describes the inputs of the components a fix would add (row 114)
- A fix carries per-component inputs, a required input without a value fails it (row 114)
- The rollup, the backfill and the reads run in the org's own calendar (row 126)
- Every trend serves the org calendar and names its timezone (row 126)
- A timezone change re-rolls the org's trend days, migration 0149 re-rolls non-UTC orgs once (row 126)
- The onboarding merge sets the project running like a fix merge, trigger type onboard (row 127)
- Cascade version pins into the platform repo chart and versions.env, idempotent re-runs

### Fixes
- Encode branch into one opaque R2 key segment (collision safety)
- Attribute the branch on GitHub pull_request runs (head_ref)
- Only a real branch-ref push may set the public default badge
- Make the prebuilt Go function self-contained for Vercel deploy
- Compile our own bootstrap so the Vercel deploy is self-contained
- Deploy via Vercel server-side build of platform/score only
- Peek JWT issuer without a signature-bearing parse path
- Default badge to flat 20px to match neighboring badges
- Bump Go to 1.26.5 for GO-2026-5856 (crypto/tls ECH leak)
- Default badge to flat 20px, not the bold 28px variant
- Wildcard-safe group: predicate + regression tests (Task 3 review)
- Org-scope portfolio policy resolution to close cross-tenant leak (Task 4 review)
- Deterministic policy order in EffectivePolicies (final review)
- Error on unknown instance; distinguish validation-unavailable from invalid token (final review)
- Strip em dashes from comments; drop unused Username field (Task 3 review)
- Preserve token expiry when the refresh response omits expires_in (Task 4 review)
- Drop unused Handler param from RequireRole (Task 5 review)
- Strip em dashes from Task 7 added lines (house rule)
- Strip em dashes; full cross-org + content-config coverage (Task 2 review)
- Portfolio-meta stales per rule D; 409 on the reachable constraint surface (Task 3 review)
- Independent graceful-shutdown budget for the jobs client (Task 1 review)
- Canonical log keys; org-scope MarkProjectLost; on-prem marker caveat (Task 3 review)
- Savepoint-degrade the ingest enqueue; loud no-tx error; tick + wrapped-tx coverage (Task 4 review)
- Transport failure never flips lost; has_plumber_yaml survives re-sync (final review)
- Reject numeric-notation IPv4 shorthand in strict mode (Task 1 review)
- Unspecified addresses (0.0.0.0, [::]) rejected in strict mode (final review S-1)
- Merged_yaml actually resolves (CI file + sha handed to ciConfig via GetFullGitlabCI)
- Case-insensitive value-guard keys; deliberate-scope note + QUESTIONS red-flag (review)
- ErrNotFound sentinel aborts-on-infra-error; em dashes; coverage gaps (Task 1 review)
- The cap bounds provider load, slot released on goroutine completion (Task 3 review)
- Cap overflow aborts the digest, always-divergent (ADR-0034 correction 2026-08-07)
- Disjoint singleflight groups, the nodigest key cannot alias (correction review)
- Out-of-order pushes create-if-absent, never transition existing issues (T2 review, I1)
- Restore what the PR #73 squash accidentally reverted (platform/score, a spec doc, the ADR-0026 fingerprint ruling)
- Every job declares its own timeout, and a dead run still records its failure
- The panel stops blaming the provider for our own interruption (gates)
- A fresh install seeds its default instance, and two lookups stop lying
- The checkpoint suite stops deleting the row it borrowed (quality gate)
- A real policy evicts the derived default, and the e2e rulings go to QUESTIONS
- The panel stops reporting dead runs as running, and River stops rescuing live ones
- Address task-3 review findings F1-F4 on the onboarded writer
- Scope ProjectsForInventory to (org, provider, instance), close cross-instance leak
- Guard 0019's down migration on pristine content, not just name (F1/F2)
- Unknown freshness classifies as the uncovered floor (final review)
- Watchdog timeout test coverage + ADR-0029 bulk-sweep carve-out (Task 1 review)
- Test the snapshot fan-out's partial-failure and query-error paths (quality gate F1)
- Env-var alternatives for -token/-client-secret (security gate F1, ruled 2026-08-11)
- Tighten 0020's DELETE with a content discriminator + test the name/org scoping (task-4 review F1/F2)
- MarkProjectsStale clears running_* provenance (ruling 2026-08-11, Task 1 review F1-F4)
- Triggerapi cross-instance enqueue leak in scope sweeps (Task 4 review F1-F4)
- Final-review findings - 409-on-running, the jobs-page bound, doc accuracy
- Harden extractComponentInclude against quoting/comments (task-1 review F1-F6)
- Strip dangling partial rune after the component-field cap (task-2 review F1)
- Demote component_path to a plain log attribute, red-flag the canonical-key question, cap it before use (task-3 review F1/F2)
- Keep stored component_outdated on a tags-lookup failure, scan tags for the max version, thread ctx into ListTags (final-review F1/F2/F3)
- Truncate snapshot timestamps to microseconds for the Postgres round trip
- Cap batch issue_ids and validate owner format (security-gate F1/F2)
- Reasons-driven 505 gating + access-level enum bound - green (F1/F2/F3)
- The duplicate_pending re-enqueue heal (final review F1)
- Require project assignment before trusting a policy's config (security-gate F1)
- Control-char rejection, server-side branch adoption, status-guarded writers (review round F1-F5)
- Reject U+0085/U+2028/U+2029 line breaks too (security-gate F1, re-review)
- Populate mr_iid/mr_url in toFixSummary, red-flag the running-SSE gap, correct the FixStatus doc (task-4 re-review F1/F2/F3)
- The 408 fixer's repo-path + FQDN bugs, the MR-mode deployment-status gate (final-review F1/F2)
- Composite-FK org backstop + status CHECK on the catalog schema (Task 1 review)
- Bound crawled-file size before parse + label count cap + coverage (Task 2 review)
- Honest persist-failure state + tree-fail worker test + activity comment (Task 3 review)
- Rescan test coverage + no-token 422 + project_id log key (Task 4 review)
- Thread the sync id into the crawl job so the returned sync completes (no orphan pending row)
- Sync-row lifecycle - rescan-in-flight 409, failed-row completion, successful-crawl nightly baseline + cleanup (final review)
- Document the trusted-id-set contract in SQL + pin list/count filter parity (Task 1 review)
- Cap the labels filter list + pin CSV parsing (Task 2 review)
- Distinguish catalog-miss from ambiguous + deterministic version fallback (pre-review rulings)
- Bound the crawled CI file path + pin the resolver's refusal branches (Task 3 review)
- Match required templates on the template path the version tags encode (final review F1/F2)
- Bound the component ref charset + reject traversal segments (Task 1 review)
- Per-item policy failures, caps, real rollback coverage and metric parity (Task 2 review)
- Bound the component item without swallowing its own first line (Task 3 re-review)
- Refuse unboundable inputs shapes instead of splicing a duplicate key (final review)
- Close the Maintainer role's partial-visibility write holes (task 5, findings 1/2/3)
- Scope the id-to-path resolution behind Finding 1's visibility check (fix round 1 blocker)
- Classify loader errors instead of echoing them
- Move the fetch cap into the seam, budget the offer, harden a test assertion
- The final-review wave
- Gate ticks on FanoutJobArgs' own uniqueness, doc JobsEnqueueFailures kind=fanout
- Rotate stuck candidates on fanout_drained_at (ruling 2026-08-15)
- 400 on an absent auto_fanout_enabled (R8-2)
- Refuse onboarding honestly on a component without the platform input (R8-1)
- Drop the raw yaml error from R8-1's stored diagnostic, pin three decode-clean shapes
- The final-review wave (yaml.v3, the amendment reword, the records)
- Review fixes - precise wait, stamp-in-the-past guard, jitter
- Cap the stamp against hostile Retry-After (gate blocker)
- The final-review wave (one window per instance, canceled metric, the records)
- Never store an unresolved uuid-shaped policy name (ADR-0033)
- Resolve requiring-policy uuids to current names on read + resolution observability (final review F1/F7)
- Absent only on a positive match + name-scoped spec memo (rule A)
- The final-review record sweep (F3-F8 + the readRawFile record)
- Migration 0035 strips unresolvable uuid-shaped requiring-policies entries (the 2026-08-17 ruling)
- ReadRawFile cap, snapshot all-degraded skip-write, rule-H transport retryability (recorded follow-ups)
- First-association by runs.received_at, migration 0037 dropped (task 1 review)
- Never enqueue for an out-of-order push (task 2 review - display never regresses)
- Adopt-by-search on nil anchor - the retry never duplicates (task 2 review F1) + the loud IID guard
- Adopt only marker-bearing issues + the severity whitelist (final review F1/F2)
- Out-of-order never fabricates - pushed score + recomputed:false (task 2 review blocker) + the doc/cap closures
- The mr-comment superseded guard + the gate lockstep test + the org-client dedup (recorded follow-ups)
- The sync tick never enqueues for a base_url-less instance (the residue generator closed)
- The #118 sweep's one-liner - TestRecheckProject_Duplicate_409 now actually exercises the duplicate path
- Qualified path lookups on every surface, badge fail-closed, the bare query deleted
- ProjectUpdateEvent carries instance; the dispatch gate drops cross-instance and empty-instance events
- The fixes tab carries its issue (issue_id + control_type - final review F1)
- Resolveapi + contextapi resolve by the verified tuple; the path-for-issuer lookup dies (ruling 2026-08-19)
- Deterministic ordering + the backfill guard (Task 1 review minors)
- Bump the pinned Go toolchain to 1.26.6 (six reachable stdlib vulns)
- A terminal-skip push never fabricates a score or claims evaluation (Task 1 review, critical)
- No provider write-back for a terminal-skip push (the skip predicate reaches the enqueue seams)
- Gate.reason's enum carries the terminal-skip states (final review C1)
- The branch validator matches git-check-ref-format's component rules (Task 1 review, critical)
- The branch prefix owns the START of the branch name, leading dash rejected (re-review)
- A lost project is unboardable, and the in-flight guard's real coverage is stated (Task 1 review)
- The include removal is scoped to the include block and removes every matching entry (Task 2 review, critical)
- No stranded unboarding, the rule-G assertion, SSE on both transitions, and the stale race comment (Task 3 review)
- A failed restore is counted, and the fixture stops hiding that path (re-review 1e)
- The in-progress guard gets its own unit test, and two stale descriptions of it (Task 4 review)
- The include removal handles zero-indent arrays and nested inputs, and refuses duplicate include keys (final review C1, C2, I4)
- The include removal verifies its own output before returning success (final probe round)
- The SMTP test send is bounded end to end, and says plainly that it proves no delivery (Task 1 review)
- The PDF renders real Unicode via vendored DejaVu Sans, marking what it cannot (Task 2 review + rulings 2026-08-20)
- The schedule is OrgAdmin-gated and attributed (Task 5 review, critical)
- The SMTP test send stops reflecting the far end and bounds where it connects (final review I4, I6)
- Rune-aware wrapping, contained generation panics, and an honest CSV (final review C1, I1, I5)
- The push response is built from the generated types
- Close the required-field, kind, and format gaps the final review found
- Restore the 30 descriptions YAML truncated, guard the document
- Review fix round 1, doc comment placement + guard limit note
- Correct the contractguard importer list, queue two follow-ups
- The org commit prefix reaches MR titles
- Explain why title and commit message stay separate (review round)
- Every operation declares its own security posture
- OperationsMissingSecurity discriminates null and non-mapping path entries
- Reap stranded pending unboardings and restore the project
- Publish SSE for the watchdog's own restored projects
- Correct the reaper's metric Help, queue two residual risks
- Keyset-paginate GET /projects, push visibility into SQL
- Composite index for the /projects keyset page/count queries
- Push visibility, fixable, and pagination for /issues and /issues/fix/history into SQL
- Close the non-string-code divergence between the SQL and Go fixable predicates
- The guard didn't work, plus an offset-overflow 500
- SQL-aggregate /overview, never load the fleet (J03-E9)
- Startup cross-check, OnboardTimeout vs the watchdog reap cutoff
- Window the analyses tab to the last 30 days, both lanes, lists and totals
- Redetected_while_dismissed reason for X10-2's suppression case
- Drop the org segment from the public badge URL
- Non-vacuous negative routing tests + at-most-once delivery doc
- Delete 3 dead queries, add the caller-loop N+1 guard (query-audit slice 5)
- Correct finding 17's caller enumeration (review fix round 1)
- Push honesty - collapse duplicate policies, drop unresolved from score (X3-15/X3-16)
- Self-hosted component detection - THE RULE (ADR 2026-08-22)
- Fix round 1 - detection compared the wrong unit (production no-op)
- GET /projects excludes archived + provider-deleted by default
- Fix round 1 - catalogapi hooked, at-most-once caveat, rename-label clarity
- Fix round 1 - the GROUPS branch of the visibility filter was untested, two doc fixes
- Delta-scope the TestConfigAuditHook_* assertions (main red since 4cb82c8)
- Batch snapshotEffectivePolicies into 2 statements regardless of N (finding 17)
- Lower GET /api/v1/audit/config's gate to Maintainer (fourth clearing, fork a)
- Keep the previous score when the newest run is unscorable
- Detect.go reads the project's own ci_config_path (GA slice 1 task 3)
- Onboard/unboard/fixers honor ci_config_path + worker-time EXTERNAL guards (GA slice 1 task 3)
- Zero-byte CI file counts as no-CI + traversal shapes refuse (final review findings 1-2)
- BumpComponentRef rewrites every occurrence, not just the first (J06-E8)
- ISSUE-408 writes the platform input, shared with onboarding (J12-H3)
- ISSUE-505 branch-access-level fixer replaces, not appends (J12-H3/J12-E4)
- FixWorker gates on project lifecycle state at execution time (J12-R4)
- Observe pass surfaces a conflicted fix MR instead of hiding it forever (J12-R3)
- ISSUE-505 refuses on identity rules, fixes a blind reason-match test (fix round 1, findings 1+3)
- Distinguish an unreadable component spec from not-declared (fix round 1, finding 2)
- Already-merged tolerance on both merge endpoints (GA slice 3, task 2 change 2)
- The F2 duplicate-onboard-window guard + GA slice 3 records (task 2 change 3)
- Log the already-merged tolerance's own GetMR failure (quality-gate review)
- GA slice 4 change 1 - snapshot_refresh at River priority 4
- GA slice 4 change 2 - the queued sync run is visible
- GA slice 4 change 3 - trigger-scoped acceptance (ADR-0019/X4-4)
- Fix round 1 F1/F1b - EnqueueSyncTx never poisons the caller's tx
- Fix round 1 F2/F4 - AuditSyncRunStatus gains queued, regen is clean
- Fix round 1 F3 - a re_check landing's own audit row isn't a zero-score placeholder
- GA slice 5 Task 1 - the tally counts identities, not occurrences
- GA slice 5 Task 2 - control version leaves the issue identity (full continuity)
- GA slice 5 Task 3 - config-side integrity (J07-H1, J07-R2, J07-H3, J12-E3)
- GA slice 6 Task 1 - absent is not empty (the systemic strict-decode fix)
- GA slice 6 Task 2 - shared Cc/Cf/Zl/Zp character validator, name/color bounds, comment character rule
- GA slice 6 Task 1 fix round - id round-trip, byte-length wording, extended strict-decode guard, strictdecode unit tests
- GA slice 7 Task 1 - org token write-scope + the mass-disappearance guard
- GA slice 7 Task 2 - instance honesty + delete, the path tie-break, the owner rule
- GA slice 7 Task 2 fix round - atomic instance-delete gate + doc touch-ups
- X3-17 - one shared guard against control bytes in path parameters
- X3-2/X3-10 - push-body nesting-depth cap + a named provenance-guard diagnostic
- X5-22 - honest /health redis check + liveness/readiness split
- X5-6 - consistent metric registration, every fixed-enum vec at zero
- Slice-8 final-review findings - regenerated types, blob-scope sentence, sweep comment honesty, em dashes, counts
- Migration 0060 self-remediates duplicate open onboardings (J02-H1 P0)
- Close the orphaned queued sync run (J05-F5)
- Merge_not_ready for a just-opened MR GitLab hasn't checked yet (GA slice R1, task 2 change 3)
- Requirements[].controls[] required fields (J07-E2's survivor, GA slice R1 task 2 change 4)
- GA slice R1 final-review fix round (F1-F3)
- Bound hung-Redis latency with a dedicated tight client
- Decode GET /config/policies as the new envelope (GA slice 9 fallout)
- GA slice 9 fix round Finding 1 - process-nonce SSE ids
- GA slice 9 fix round Finding 2 - three list-discipline stragglers
- GA slice10 fix round 1 - cap the not_evaluable blob fan-out, surface read failures honestly, fix the next_run_at contract lie
- Thread PLUMBER_RETENTION_DAYS into the startup backfill floor (ADR-0022)
- GA slice 11 fix round 1 - within-window delete blocker + N+1 read-path record (final-review.md)
- Eligibility re-derived from observed visibility (public only)
- Component_outdated reaches the wire (J05-E2)
- Sync tells the truth - adoption, anchor-gone message, demote observability
- The save path never creates a zero-control policy (J06-E2)
- Same-org DB backstop on SetProjectBadgeEnabled (S-M1)
- Migration 0080's down is safe with a live project_badge audit row (Q-M1)
- Correct migration_guard allowlist comment off-by-one (final-review Q7)
- Https-only + no-redirect PLUMBER_UPDATE_CHECK_URL, log the fetch error (final-review S1, Q5)
- Percent-encode the project path in the platform deep link (J13-H1)
- Warn in the MR description when a written include targets a non-public catalog repo (J12-E5)
- Report the label-cap truncation, protect the size-limit/traversal refusal reporting (J09-S1)
- Rank served catalog versions with the fixer's own go-version parser (fix round 1 MAJOR-2)
- Report over-long label drops per-template (fix round 1 MINOR-1)
- Bound the interpolated visibility text in the MR warning (fix round 1 MINOR-3)
- Refuse link-local unconditionally (X2-4)
- Strip trailing slashes from PLUMBER_BASE_URL (J01-E4)
- Route undefined_table to the migrations-first message, exit 2 (J01-E3)
- Interpolate the real base URL in the login hint (J01-F5)
- Redact PLUMBER_REDIS_URL credentials from every error/log path (rule G)
- X1-6 - 503 for session/role-derivation infra failures, never 401/403
- X5-5 - crash-idempotent CommitFile, resume via content comparison
- X7-4 - createPush's honest response codes, unmask 413
- Fix round 1 - unconditional link-local coverage for numeric hosts, strict redis scheme validation (final review MAJORS 1+2)
- Attach canonical provider/instance keys to the component resolve check's own log lines
- Guard project query-param filters against NUL bytes (X3-18)
- GA slice 16 fix round 1 - the five majors (S1/Q1/Q2/Q3/Q4) + Q5
- I6 vocabulary change - drop 'failed' (GA slice 17 Task 1)
- J06-F1 - policy_ids becomes key-required, empty-allowed (GA slice 17 Task 1)
- Wire policy + portfolio score headers to the slice-11 shared compute (GA slice 17 ruling J17-H1)
- GA slice 17 fix round 1 - vocabulary doc catch-up + contract fixes (Q1-Q7)
- Fix round 1 - dogfood score, self-counting gate, breaking changelog, PR push auth
- The honest, complete snapshot payload (Task 1, ADR-0032-adjacent slice)
- One shared issuer canonicalization for both projects.issuer writers (Task 2)
- Warn on live issuer-identity divergence, count already-split rows at startup (Task 2)
- Batched per-policy control tree read (Task 3, point 7 ruling (a))
- Serve the per-policy control tree on /context (Task 3, point 7 ruling (a))
- Path-qualified split diagnostics, ingest-tx index, verbatim control config
- Name PLUMBER_SYNC_TIMEOUT in the abandoned-run error + document the sync estate ceiling (X5-21)
- Shared TimeoutKnobHint on the classifier's dead-context branch (X5-21 fix round)
- Discovery-path fixed messages hint the knob on a dead context (X5-21 residual)
- Make migration 0086's users.username add idempotent
- Make migration 0087's onboarding_done add idempotent
- Reject tokenless group scope and admit rescue of the dead pending state (spec H)
- Resolve a token-only rescue's preexisting group scope (spec H review)
- Seed a fresh org per test asserting derived signals
- Gate config.update and sync.update delivery by connection role
- Correct CountProjectsForInventory's stale LATERAL comment
- Avatar_url nil-when-absent, doc/label accuracy fixes
- Branch_protection arrays never marshal as null (#368 bug a)
- Cap include probe values before the outbound call, not just the memo key
- Cap probe fan-out, fix memo-key and nil-includes bugs
- Rewrite identity_test.go golden vectors for recipe v4
- Rewrite the 3 failing engine identity tests for recipe v4
- Serve source_catalog present-but-empty for non-catalogue projects (#368)
- Merge settings zero-values test + gofmt
- Drop the fictitious policy_rules union from policy_ids/policy_id
- Strict-query-check GET /projects/:id (listqueryguard finding)
- Derived-count dismiss assertion, strict-query on instances, guard coverage
- Honest issue-trend limit-guard entry, correct route-order comment
- Replace leg for the control rollup, smart-quote and changelog cleanups
- Review round 1 - smart quotes, execrows, malformed expiry test
- Prove usage.project_ids never null on the wire, doc fixes
- Gate ai completeness on resulting enabled state, not request presence
- Final-review truth pass on licence and ai records
- Genuine 404 is honest absence, not degradation (j05-f6)
- Control config write path preserves raw bytes (j07-f3)
- Cap pushed missing_fields and restore null-config rejection
- Synced-groups envelope adopts full page/per_page shape
- Close the name-clobber landmine and the migration trio gap (item 3)
- Final-review hardening on the detection clear leg and synced-groups scope truth
- A policy declaring no controls covers nothing (x3-16 ruling)
- Cap raw_config at fetch and serve source_catalog once per target
- Bound min_points above to prevent int32 wraparound
- Batch the policy as-of compute, killing the per-result-row correlated subquery (item 10)
- Final-review observability polish (canonical keys, gate dial distinction)
- Hard-gate the projects sort EXPLAIN check and fix the Seq Scan it caught
- Preserve invisible-instance official rows and fix version-check contract wording
- Final-review wave (guard entry, explain trio, records truth)
- Fence the direct resolution against the project's org (review S1)
- Scope-preview review round 1 (visibility, required filter, membership guard)
- Final-review wave (allowlist entry, excluded_count truth, guard floor)
- Branch-model display ownership review round 1 (F1, F2, F3, F4, F6, F8)
- Seed default_branch across the checkpoint rails (spec s3, fix round 1)
- Root-cause the shared-dev-DB residue (spec s3, fix round 2)
- Honest sort-key trailing comment + branches tiebreak amendment (task 5 fix round 1)
- Concurrent index build and sqlc drift (task 6 fix round 1)
- Renumber the worst-branch migration 0118 to 0120 (task 7 fix round 1)
- Org-fence the trio write, resilient aging loop (task 7 fix round 2)
- Final-review wave (gates, phantom branch, report truth, concurrent indexes)
- Write the history to a data branch, not to main
- Partial index and explain gate for the dismissed-identities context read
- Restore the frozen pre-s6 breakdown oracle and assert its values (task 1 fix round 1)
- Omit the score pair on a failed degraded lookup (row 14)
- Assert scoreDegradedByIDs known/nil mapping (row 14 fix round 1)
- Coverage follows governed membership (row 15)
- Row-16 fix round 1 - eligibility comment correction and two new-path pins
- Tree read fix round 1 - full visibility in the group aggregate (S1)
- Correct the tree-read deep-node cost to its single measured datum
- Final-review wave (deterministic tree gate, mr-comment truth, visibility doc lockstep)
- Stored has-results flag for as-of run selection (row 18)
- Only onboarded projects go stale, repair the rest (freshness honesty)
- Restore row-18 changelog header, limit-guard allowlist, honest stale-repair predicate
- Removal closes issues without staling the not-yet-onboarded project (honest freshness)
- Recheck sweep skips non-onboarded projects, matching the stale count (issue 91)
- Keep policy-projects sort index-driven, no seq scan on scope-filtered read
- One policy resolver per row, plain-ascii test comment, ref fallback test (ask 26 review)
- Validate display url scheme, repair the severity explain guard, harden provenance export (final review)
- Describe the policy freshness summary over the real governed set, portfolios included (ask 22)
- Cache cli_version at init, state the go-test empty case, refresh the guard survey doc (ask 24)
- Cover the mapping's untested branches, name the type-mapping panic, make elem nameless (ask 24)
- Read the issue code through the one existing reader, drop the stale join sentence (row 30)
- Stale only what a write affects: metadata stales nothing, membership only entering and leaving
- Apply the governed-set stale rule to the granular writes and lock the policy row on meta
- Stale on a change of governance, and apply the governed-set rule to every policy-side write
- Take the onboarding policy-row locks in a deterministic order
- Drop the obsolete gitlab token warning and prerequisite, the component now runs token-free
- And freshness-unchanged across onboard merges, restore freshness doc (row 33)
- Lock policy rows before the portfolio row in portfolio-side policy replace (row 34)
- Refuse a portfolio policy replace whose lock set changed under it (row 34 review)
- Honor applies_to override in policy_id preview, tighten row 35 coverage
- Stop preview defaulting applies_to to assigned when absent
- Re-correlate the enriched projects page latest-run join (row 35)
- Run the applies_to rule-9 diff only on a real flip, correct the governed-set and rule-N prose
- Split the policy projects page into assigned and all_onboarded query families (row 35)
- The all_onboarded page family agrees with the governed set, direct rows restored (row 35)
- Convert re-check audit copies and read an old CLI's malus pair (row 40)
- Serve dismissed_issues.recipe_version from canonical_identity, not hash_version
- Never fail /context on a version-less canonical identity, correct the dismissal plan's wire note
- Drop never-onboarded projects from the config-write affected list (row 38)
- The trigger adapter stales languages inside one transaction (row 37)
- The onboarding MR's allow_failure sees all_onboarded block policies (row 39)
- Rule 9 and the governed diff resolve both sides across every instance of the org (row 40)
- Apply the scope filter to the row-39 as-if-onboarded forecast leg
- Row 36 fix round 1: governed policy_count, concurrent index, stable sqlc doc, EXPLAIN gates
- Keep getPortfolio's resolved preview on its pre-row-49 default (F2)
- Render no score instead of a bare 0 for an unscored run (row 45)
- Renumber the score_withheld migration to 0131, main took 0130
- Normalize the component ref to v-prefix and degrade on write failure (row 58)
- Unscorable runs leave the project, org and portfolio as-of averages (row 55)
- The hourly rollup fills every missing day from the org's watermark (row 13)
- Keep unknown control types unvalidated and judge integers by int64 (row 56)
- Count a pushed grade on a withheld policy, make the breakdown gate order-independent (row 57)
- Report a non-integer number on an integer field as a type mismatch (row 56)
- Repair loop-guard allowlist, label set, and cap row 56 violations
- Cap the root CI file fed to the multi-document parser and pin its degrade
- A nothing-evaluated re-check records itself and never blanks the pre-existing run (row 65)
- Refresh the component ref only for a display-owning push (row 68)
- Canonicalize control config keys before validating and storing them (row 66)
- Import only the keys the .plumber.yaml actually wrote (row 66)
- Break the storage test import cycle left by the cli version stamp (row 67)
- A tolerated key keeps its control's pin stamp so the toleration outlives one save (row 67)
- A component re-detect that did nothing reports itself as skipped, never completed (row 69)
- Count and warn on a skipped import control, name the legacy non-match rule (row 66)
- Say why a stored config was judged verbatim, and state what cli_version really records (row 67)
- Bound the recursion of migration 0134's re-key so a pathological config cannot abort boot
- Migration 0134 prunes only what encoding/json invented, on rows it re-keyed
- Close the F2-F7 minors from the 0134 quality gate review
- Store version_components under the served spelling and keep enrichment on a fallback run (row 70)
- Register the excluded-projects route with the audit guard and validate only newly excluded ids (row 71)
- Cap the per-version components selection and pin the excluded_projects array shape (row 70, row 71)
- An unmarked re_check that pushed nothing scorable keeps the display-owning run (row 74)
- Attribute a re_check's issue pass to the display-owning run (row 73)
- Never refresh a branch, ~latest or sha component pin from a push (row 76)
- Refuse to start when the pinned CLI version is unknown to the binary (row 79)
- Official explain arm asserts no seq scan, contract and comments name gitlab_maintained (row 82)
- A provider-deleted project no longer consumes a licence seat (row 89)
- Provider-deleted projects leave portfolio membership and resolved counts (row 89)
- Membership reconciles read the unfiltered explicit set so a provider-deleted member keeps its row (row 89)
- The org score rollup ignores provider-deleted projects (row 89)
- Re-check answers 409 for an archived or provider-deleted project (row 89)
- The row 91 explain gate pins the two-column index condition, 0138 header and contract bullet corrected
- Page adapters carry resolved_component_ref, tag resolve transport failures retry (row 90)
- Overage signals read the tier in effect, resolved tag charset gate, gate records (quality gate)
- Actor_id omitted exactly when actor_name is absent, config lane serves the avatar (quality gate)
- The onboarding pipeline read counter joins the observability registry, notes corrected (row 103)
- Cap the pipeline jobs scan, pin the claim-time actor, log mr_iid, refresh two comments (gates)
- The template and component fixers accept the one-issue-per-rule finding shape (row 107)
- Required-include fixes read the expression, not the retired field
- Only the Plumber component gets the platform input
- The fix worker says loudly when the instance component path is unknown (row 115)
- A malformed group answers 400 at trigger, an unreadable component names the token (rows 116, 117)
- Required expression bounded, console merges touch activity, touch after commit (gates)
- The worker pool never outgrows the database pool, a recovered panic warns the run, test gates scoped (review)
- The no-result pool rotates oldest-first, the detail states what the platform saw, clears tested (review)
- The worker ceiling holds for a hand-built configuration, doc lines reflowed, a test name says what it proves
- Sibling runs wait on the same observation, an abandoned wait still lands its observation (review)
- Awaited waits bounded, the gate keeps component_outdated current, stamps on failure (gates)
- Outdated refresh walks every skipped project, a straggler counts its wait, suppression reasons
- The applies_to alias is canonical at read too, a rollback tolerates both spellings (review)
- An all policy governs live projects only and the fleet-scale reads are named (review)
- All-policy tests own their org, the flip cost named honestly, stale comments rewritten (gates)
- A landed observation ends its cool-off, a no-slot abandonment arms one, labels corrected (review)
- The activity touch is org-scoped at its nine call sites (hygiene)
- A pre-warm stamp ends the cool-off, EXPLAIN skips judged only when the gates ran (review)
- The no-slot arming cannot wait forever, the gate step counts what ran, three doc lines (gates)
- A moved pin resets its version check, the redetect legs and the counter are tested (review)
- The fix-inputs read is bounded in time and memory, a failure is cached one minute, options are honest
- Input names and nested values are bounded, one shared spec description (review)
- A moved pin resets the in-memory pair too, plus three review residuals
- A string-typed input is always written as text, the name grammar accepts a leading digit (review)
- The fix inputs read is bounded, gated like the trigger and counted (gates)
- The input name refusal is scoped to required inputs, an optional non-plain name keeps a fix working (review)
- Re-roll clears only recomputable days, walk refills, counted after commit (review)
- The fixer reads a component spec once per entry, the read's defensive exit is counted (H5 hygiene)
- Midnight-DST cutoffs, honest tick summary, no-UTC 400s (review)
- Postgres checks the zone, the settings write locks, docs follow the org calendar (gates)
- Default update-check manifest lives in getplumber/plumber-platform

### Performance
- Add the indexes the query audit found missing
