

### Features
- A failed earliest-run read is counted, the walk floor narrowing has a signal (row 126)

### Fixes
- A refresh that learned nothing never overwrites the last good snapshot, a proxy's 404 included (X5-F3)
- A limiter wait names the provider window and never dies of the client timeout (X5-F2)
- Migrations 0115 and 0127 commit the column add before the conversion, readers survive the rewrite (J02-R3)
- A window-starved run says so even when its timebox expired, a deadline-less wait is bounded (review)
- Dead guard removed, starved-run sentence split, four migration test fixes (review)
- The provider client caps redirects and keeps the token on its host, the docs say what ships (gates)
- The redirect token rule mirrors Go's own same-origin test, the ceiling sentence names the hop cap (gates)
- The hop-cap refusal is fixed text, the changelog lead-in counts both sentences (re-review)
- Zero-indent and empty include lists stay valid, a clean ref, stage only if declared (J06-E8)
- A branch pin keeps its slash, the undeclared-stage warning has no wire value (review)
- The branch protection fixer fails honestly when the provider discarded a field it answered 200 for (J12-F7)
- The writer refuses include styles it cannot edit, a refused ref clears the stored pin (review)
- A 404 from the component repository names the org token, the sentinel client-go returns (J12-F6)
- Every include form the writer does not edit is refused, three more shapes pinned (review)
- The organization export carries the org score, unscored only without data (X6-F2)
- Every missing key named, sentence order pinned, decode leak fixed, comment tolerated
- Mapping-form and duplicate includes refused, quoted keys handled (gates)
- A leading BOM is preserved, any top-level key ends the block, the output is proven (gates r2)
- An unparseable file has its own word, the proof claim scoped (gates r3)
- The observation-done hook fires after the in-flight guard and the awaited slot are released (H4 seam)
- A guarded refusal vocabulary, the loader error logged, unproven write named
- The assembler takes the process's one location resolver (row 126)
- Priority test owns its queue, four comments true (reviews)
