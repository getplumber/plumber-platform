

### Features
- Issue rows carry display.missing_groups from the finding data (ask 58)
- Members refresh their own cached access from the sync panel

### Fixes
- Every merge sends the mr head sha, a 400 is merge_failed (customer install)
- The projects search matches the display name as well as the path
- The list editor keeps its draft so a new line can be entered (customer install, critical)
