

### Features
- The allow-list derives from the instances, readyz names the rest, locator keeps the path
- Unresolved policy names are unique per run, the boundary collapse gains a backstop (row 173)
- A provider absence reason beside the flag, the push kind stable across retries (rows 149, 172)
- Sync status carries attempt n of m and the last error class while a run retries (row 147)
- A manual sync bypasses the activity gate, the head sha breaks ties (row 164)
- A pass records its attempt, an all-404 pass on a known project is an outage (row 191)
- The sync rides its own provider window, additive to the background one (row 192)
- The provider absence reason is written, deleted, hidden and a bounded transferred read (row 149)
- The writer converts a single string or mapping include, the readers learn the key spellings (row 201)
- Onboarding lints before it writes, picks a stage, records warnings (rows 165, 182)
- An instance that discards premium protection fields refuses those reasons up front (row 162)
- A re-check with no prior run leaves a readable outcome on project detail (row 144)
- Re-point a group-scoped instance's root group and let sync resolve it (row 166)
- A bounded read-only grace window from the role cache during a provider outage (row 190)

### Fixes
- Every outbound client refuses private and link-local addresses at dial time (row 146)
- A 60 s clock-skew leeway and a platform-side maximum token age (rows 178 and 170)
- A token without iat is refused while the maximum age is enforced (row 170)
- A control byte in any declared query value 400s centrally, never the driver (X3-24)
- A malformed path answers the same 404 body as every other refusal (row 143)
- The provider-token read requires Member, a role-none login gets the same 403 as every read (row 141)
- The derived issuer set expires on the ttl for hits too, a removed instance stops ingesting (row 142)
- The queries allowlisted, readyz counts without naming (reviews)
- The include probe keeps the url prefix, the issuer refresh survives a canceled push (gates)
- The mass-disappearance guard fires on either threshold below 33 projects (row 148)
- The no-result observation also reads in-progress projects (row 167)
- Below the ceiling the percent leg needs two vanished projects, the warning names the knobs (row 148)
- A plumber file that yields nothing importable is an unavailable offer, never silence (row 150)
- The onboarding writes decode strictly like every other write (row 153)
- The cli.version fallback fills an empty pin only, a resolvable include ref is never overwritten (row 157)
- A second fix trigger is refused while the first is open, pending or mr_opened (row 160)
- The fix-uniqueness cleanup keeps the open fix with the merge request, ties to the newer (row 160)
- Row 58 push tests follow row 157, stale comments and 415 wording corrected (reviews)
- A push lands under the instance its issuer names, the sync corrects a defaulted tenancy (J03-F4)
- Ambiguity tested, whitespace guarded, the tenancy warn is gitlab-only and counted (J03-F4)
- The attempt trio is served only while a run is live, the publish points name it (row 147)
- A re-onboard answers already_pending on a live duplicate and cancels a dead worker's job (row 194)
- The head-sha pool rotates on a fairness cursor, the read carries the deadline (row 164 review)
- The outage log keys are proven, the row 191 comments say the ruled state (review)
- The absence pool rotates on a fairness cursor, hidden never names a single vanish (row 149)
- Ambiguity case precedes provider gate, comments and sync window remedy fixed (reviews)
- The include detection parses the CI file as YAML, the line scanner is the fallback (row 169)
- A comment below a converted mapping keeps its column, a hash in a scalar is not a comment (row 201)
- The include walk follows aliases, a duplicate key falls back, only the first document is read (row 169)
- A project without stages gets the default test stage, the undeclared note tells the truth (row 165)
- A no-op premium write teaches nothing, the probe's wiring and rules are tested (row 162)
- The policy score averages the governed set (row 132)
- A push naming an unassigned policy is stored but never reaches the global score (row 171)
- The all policy counts live projects, never materialises the org on write (row 137, J07-F4)
- Portfolio membership staling needs a policy (row 135)
- Duplicate control-group names are 422, scope-filter prose fixed (rows 138, 136)
- The licence cap counts only new seats (J15-F2)
- An unresolved root group exits 1 and the with-token dead state is rescued (rows 174, 195)
- The re-point diagnostic uses the message key and sync words its own pending failure (review)
- A definite provider refusal of the user's own token answers 403 with an actionable body (row 140)
- The eight 403 variants carry the refusal clause, a definite refusal logs at warn (review)
- Visibility resolution failures answer 503 or the actionable 403, never insufficient role (row 129)
- The scope resolver doc names the classifier, the fix inputs refusal keeps a string body (review)
- The grace counter is pre-seeded, the event stream is grace-served (review)
- The gates round, org fence on the re-point, grace pinned, path bound, label (gates)
- The control configuration leaves the issue identity, migration 0162 merges the twins (ruling R1)
- Migration 0162 keeps a dismissed or in-progress survivor when merging identity twins (review)
- Migration 0162 closes the tombstoned twins and carries their runs to the survivor (review)
- Filter issues by issue type and match q on the code (issues page review 1 and 2)
- The issue code index is org-scoped like the severity index (review)
- The trend query carries the issue type predicate once (review)
- Github-only controls are masked on this gitlab platform and dropped on write (R2)
- Honest refusals and labels for masked controls, the issue types sentence (review)
- The policy row carries its governed project count (Thibaud ask 57, ruling R3)
